import { useEffect, useRef, useState } from 'react';
import Quagga from '@ericblade/quagga2';

export default function Scanner({ onScanSuccess, onClose }) {
  const scannerRef = useRef(null); const detectedRef = useRef(null); const [error, setError] = useState(null); const [scanning, setScanning] = useState(false);
  useEffect(() => { if (!scannerRef.current) return undefined; let active = true; Quagga.init({ inputStream: { type: 'LiveStream', target: scannerRef.current, constraints: { facingMode: 'environment', width: { min: 640, ideal: 1280, max: 1920 }, height: { min: 480, ideal: 720, max: 1080 }, aspectRatio: { ideal: 4 / 3 } }, area: { top: '20%', right: '10%', left: '10%', bottom: '20%' } }, decoder: { readers: ['ean_reader', 'ean_8_reader'], multiple: false }, locate: true, numOfWorkers: typeof navigator !== 'undefined' ? Math.min(navigator.hardwareConcurrency ?? 2, 4) : 2, frequency: 12 }, (initError) => { if (!active) return; if (initError) { const message = initError?.message ?? ''; if (message.includes('Permission') || message.includes('NotAllowed')) setError('Permiso de cámara denegado. Actívalo en la configuración del navegador.'); else if (message.includes('NotFound') || message.includes('DevicesNotFound')) setError('No se encontró ninguna cámara compatible.'); else setError('No se pudo iniciar la cámara. Inténtalo de nuevo.'); return; } Quagga.start(); setScanning(true); }); Quagga.onDetected((data) => { if (!active) return; const code = data?.codeResult?.code; if (!code) return; const errors = (data.codeResult.decodedCodes ?? []).filter((item) => item.error !== undefined).map((item) => item.error); const averageError = errors.length ? errors.reduce((sum, value) => sum + value, 0) / errors.length : 1; if (averageError < 0.18 && code !== detectedRef.current) { detectedRef.current = code; active = false; Quagga.stop(); onScanSuccess(code); } }); return () => { active = false; Quagga.stop(); Quagga.offDetected(); }; }, []);
  return <div className="fixed inset-0 z-[60] flex items-end justify-center bg-[#050f0a]/90 p-0 backdrop-blur-sm sm:items-center sm:p-4" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }} role="dialog" aria-modal="true" aria-label="Escáner de código de barras"><div className="w-full max-w-[390px] overflow-hidden rounded-t-xl border border-white/15 bg-[#0d1f14] sm:rounded-xl"><header className="flex items-center justify-between border-b border-white/10 px-5 py-4"><div><p className="text-[0.9375rem] font-bold text-white">Escanear código</p><p className="mt-0.5 text-[0.8125rem] text-white/40">Apunta al código de barras del producto</p></div><button className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-white/[0.07] text-white/50 transition-colors hover:bg-white/[0.13] hover:text-white" onClick={onClose} aria-label="Cerrar escáner"><svg className="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2} aria-hidden="true"><path strokeLinecap="round" strokeLinejoin="round" d="M6 18L18 6M6 6l12 12" /></svg></button></header><div className="relative aspect-[4/3] overflow-hidden bg-black"><div ref={scannerRef} className="absolute inset-0 h-full w-full [&_.quagga-viewport]:absolute [&_.quagga-viewport]:inset-0 [&_.quagga-viewport_video]:absolute [&_.quagga-viewport_video]:inset-0 [&_.quagga-viewport_video]:h-full [&_.quagga-viewport_video]:w-full [&_.quagga-viewport_video]:object-cover [&_canvas.drawingBuffer]:hidden" />{scanning && !error && <div className="pointer-events-none absolute inset-0 flex items-center justify-center"><div className="relative h-36 w-56"><i className="absolute left-0 top-0 h-[22px] w-[22px] rounded-tl-sm border-l-2 border-t-2 border-green-300" /><i className="absolute right-0 top-0 h-[22px] w-[22px] rounded-tr-sm border-r-2 border-t-2 border-green-300" /><i className="absolute bottom-0 left-0 h-[22px] w-[22px] rounded-bl-sm border-b-2 border-l-2 border-green-300" /><i className="absolute bottom-0 right-0 h-[22px] w-[22px] rounded-br-sm border-b-2 border-r-2 border-green-300" /><i className="absolute left-2 right-2 top-1/2 h-px animate-[scanLine_1.8s_ease-in-out_infinite] rounded bg-green-300 shadow-[0_0_8px_2px_rgba(74,222,128,0.55)]" /></div></div>}{error && <div className="absolute inset-0 flex flex-col items-center justify-center bg-black/75 p-6 text-center"><div className="mb-3 flex h-12 w-12 items-center justify-center rounded-lg bg-red-500/10 text-red-300"><svg className="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5} aria-hidden="true"><path strokeLinecap="round" strokeLinejoin="round" d="M12 9v3.75m9-.75a9 9 0 11-18 0 0 1 18 0zm-9 3.75h.008v.008H12v-.008z" /></svg></div><p className="mb-1 text-sm font-semibold text-white">Cámara no disponible</p><p className="text-[0.8125rem] leading-relaxed text-white/50">{error}</p></div>}{!scanning && !error && <div className="absolute inset-0 flex flex-col items-center justify-center gap-3 bg-black/50"><div className="h-8 w-8 animate-spin rounded-full border-[2.5px] border-green-300/20 border-t-green-300" /><p className="text-sm font-medium text-white/70">Iniciando cámara…</p></div>}</div><footer className="border-t border-white/10 p-4"><button className="w-full rounded-lg border border-white/10 bg-white/[0.05] px-4 py-3 text-sm font-semibold text-white/65 transition-colors hover:border-white/20 hover:bg-white/[0.09] hover:text-white" onClick={onClose}>Cancelar</button></footer></div></div>;
}
